import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  header: {
    padding: '1em',
    textAlign: 'center',
    backgroundColor: 'white',
    color: '#000',
  },
  button: {
  position: 'absolute',
  right: '10px',
  top: '0'
},
  link: {
  color: '#000',
  textDecoration: 'none'
}
}))