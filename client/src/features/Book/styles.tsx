import { makeStyles } from "@material-ui/styles";

export default makeStyles((theme) => ({
  bookCover: {
    width: '350px',
    height: '300px',
    objectFit: 'contain'
  },
  downloadlink: {
    color: '#fff',
    textDecoration: 'none',
  }
}) )